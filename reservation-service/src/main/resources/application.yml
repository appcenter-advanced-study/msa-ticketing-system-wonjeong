server:
  port: 8081

spring:
  application:
    name: reservation-service
  datasource:
    url: jdbc:mysql://localhost:3306/ticketing_system
    username: root
    password: test1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true