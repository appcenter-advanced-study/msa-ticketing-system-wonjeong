server:
  port: 8083

spring:
  application:
    name: stock-service
  datasource:
    url: jdbc:mysql://localhost:3306/ticketing_system
    username: root
    password: test1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true